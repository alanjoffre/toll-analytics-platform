global:
  scrape_interval: 15s

scrape_configs:
  # Métricas do Airflow: ele emite StatsD -> statsd-exporter -> /metrics aqui.
  - job_name: airflow
    static_configs:
      - targets: ["statsd-exporter:9102"]
