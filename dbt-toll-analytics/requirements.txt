dbt-duckdb>=1.8,<2.0
duckdb>=1.0
sqlfluff>=3.0,<4.0
sqlfluff-templater-dbt>=3.0,<4.0
# Semantic Layer / MetricFlow — necessário só para CONSULTAR métricas (mf query),
# não para o dbt build. Ex.: mf query --metrics revenue,suspect_rate --group-by metric_time__day
dbt-metricflow>=0.13,<1.0
# Geração e verificação da documentação Word (docs/gerar_documentacao.py, check_docs_sync.py)
python-docx>=1.1,<2.0
